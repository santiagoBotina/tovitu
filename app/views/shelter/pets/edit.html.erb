<div class="w-full max-w-3xl mx-auto">
  <%= render "shared/section_header",
             title: t(".title"),
             subtitle: t(".subtitle"),
             back_link_path: safe_back_path(shelter_pet_path(@pet)),
             back_label: t(".back") %>

  <div class="bg-white rounded-xl shadow-sm border border-neutral-200 p-8">
    <%= render "form", pet: @pet %>
  </div>
</div>
