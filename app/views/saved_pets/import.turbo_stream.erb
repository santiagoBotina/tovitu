<%# The import now runs in the background (FavoritesImportJob). The client is
    told immediately that the favorites are being imported; the Saved pets page
    shows the server-driven progress notice while it runs. %>
<%= turbo_stream.append "flash-container" do %>
  <%= render "shared/milestone_toast", message: t("saved_pets.import.importing") %>
<% end %>