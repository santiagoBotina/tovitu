<div class="w-full max-w-md mx-auto">
  <div class="bg-white rounded-xl shadow-md p-8">
    <h1 class="font-display text-4xl font-bold text-center text-neutral-900 mb-8">Edit profile</h1>

    <%= form_with model: @user, url: profile_path, method: :patch, class: "space-y-6" do |f| %>
      <%= render "shared/error_messages", resource: @user %>

      <div>
        <%= f.label :name, "Full name", class: "block text-sm font-medium text-neutral-700 mb-1" %>
        <%= f.text_field :name, required: true,
              class: "w-full px-4 py-3 border border-neutral-300 rounded-xl shadow-sm focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-primary-500 text-neutral-800 placeholder-neutral-400 transition" %>
      </div>

      <div>
        <%= f.label :email, "Email address", class: "block text-sm font-medium text-neutral-700 mb-1" %>
        <%= f.email_field :email, required: true, autocomplete: "email",
              class: "w-full px-4 py-3 border border-neutral-300 rounded-xl shadow-sm focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-primary-500 text-neutral-800 placeholder-neutral-400 transition" %>
        <% if current_user.verified? %>
          <p class="text-xs text-success mt-1 font-medium">Verified</p>
        <% else %>
          <p class="text-xs text-warning mt-1 font-medium">Pending verification</p>
        <% end %>
      </div>

      <%= f.submit "Update profile", class: "w-full bg-primary-500 text-white font-semibold py-3 px-4 rounded-xl hover:bg-primary-600 active:bg-primary-700 transition cursor-pointer shadow-sm" %>
    <% end %>
  </div>

  <div class="mt-8 text-center">
    <%= link_to "Back", root_path, class: "text-sm text-neutral-500 hover:text-neutral-700 transition" %>
  </div>
</div>
